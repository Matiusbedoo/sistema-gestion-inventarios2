{
    "version": 2,
    "builds": [
      { "src": "prisma/schema.prisma", "use": "@prisma/cli" },
      { "src": "*", "use": "@vercel/node" }
    ],
    "env": {
    },
    "functions": {
    },
    "routes": [
    ],
    "installConfig": {
      "prisma": {
        "ignore": "prisma/schema.prisma"
      }
    },
    "build": {
      "scripts": {
        "prerun": "prisma generate"
      }
    }
  }